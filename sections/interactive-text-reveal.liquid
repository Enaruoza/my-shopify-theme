{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  @media screen and (max-width: 749px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top_mobile }}px;
      padding-bottom: {{ section.settings.padding_bottom_mobile }}px;
    }
  }

  .interactive-text-section {
    position: relative;
    z-index: 10; /* Ensure it sits above the curtain hero */
    background-color: {{ section.settings.bg_color }};
    overflow: hidden;
  }

  .interactive-text-wrapper {
    max-width: {{ section.settings.max_width }}px;
    margin: 0 auto;
    padding: 0 2rem;
    display: block; /* Changed from flex to block for natural flow */
    text-align: center;
    line-height: 1.1; /* Tight line height for the whole block */
  }

  .it-text-segment {
    font-family: var(--font-heading-family);
    font-weight: 800;
    /* Responsive font size using clamp */
    font-size: clamp({{ section.settings.font_size_mobile }}px, 8vw, {{ section.settings.font_size_desktop }}px);
    line-height: 1.1;
    color: {{ section.settings.text_color }};
    text-transform: uppercase;
    letter-spacing: -0.02em;
    transition: color 0.3s ease;
    display: inline; /* Inline for natural wrapping */
  }

  .it-reveal-button {
    display: inline-flex; /* Inline-flex to sit within text */
    align-items: center;
    justify-content: center;
    white-space: nowrap;
    overflow: hidden;
    max-width: 0;
    opacity: 0;
    transform: scale(0.9);
    transition: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
    vertical-align: middle; /* Align with text */
    margin: 0;
    height: 1em; /* Match text height roughly */
  }

  .it-reveal-button.is-visible {
    max-width: 400px; /* Increased limit for larger buttons */
    opacity: 1;
    transform: scale(1);
    margin: 0 0.2em; /* Add spacing when visible */
  }

  .it-btn-inner {
    background-color: {{ section.settings.button_bg }};
    color: {{ section.settings.button_text }};
    /* Customizable padding */
    padding: {{ section.settings.button_padding_v }}em {{ section.settings.button_padding_h }}em;
    border-radius: 100px;
    /* Customizable font size ratio */
    font-size: {{ section.settings.button_font_ratio }}em; 
    font-weight: 600;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    transition: transform 0.2s ease, background-color 0.2s;
    display: flex;
    align-items: center;
    gap: 0.5em;
    height: auto;
    line-height: 1;
    /* Minimum width to prevent it from being too small */
    min-width: {{ section.settings.button_min_width }}px;
    justify-content: center;
  }

  .it-btn-inner:hover {
    transform: translateY(-2px);
    background-color: {{ section.settings.text_color }};
    color: {{ section.settings.bg_color }};
  }

  /* Arrow Icon */
  .it-btn-icon {
    width: 1em;
    height: 1em;
    fill: currentColor;
  }

  /* Subtext */
  .it-subtext {
    max-width: 600px;
    margin: 40px auto 0;
    text-align: center;
    font-size: 1.1rem;
    color: {{ section.settings.text_color }};
    opacity: 0.7;
    line-height: 1.6;
  }

  .it-read-more {
    display: inline-block;
    margin-top: 24px;
    font-weight: 600;
    text-decoration: underline;
    text-underline-offset: 4px;
    color: {{ section.settings.text_color }};
  }
{%- endstyle -%}

<div class="interactive-text-section section-{{ section.id }}-padding">
  <div class="interactive-text-wrapper" id="InteractiveText-{{ section.id }}">
    {%- for block in section.blocks -%}
      {%- case block.type -%}
        {%- when 'text_segment' -%}
          <span class="it-text-segment" {{ block.shopify_attributes }}>
            {{ block.settings.text }}
          </span>
        
        {%- when 'reveal_button' -%}
          <div class="it-reveal-button" {{ block.shopify_attributes }}>
            <a href="{{ block.settings.link }}" class="it-btn-inner">
              {{ block.settings.label }}
              <svg class="it-btn-icon" viewBox="0 0 14 14"><path d="M2 7h10M8 3l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </a>
          </div>
      {%- endcase -%}
    {%- endfor -%}
  </div>

  {%- if section.settings.subtext != blank -%}
    <div class="it-subtext reveal-on-scroll">
      {{ section.settings.subtext }}
      {%- if section.settings.read_more_link != blank -%}
        <br>
        <a href="{{ section.settings.read_more_link }}" class="it-read-more">
          {{ section.settings.read_more_label | default: 'Read More' }}
        </a>
      {%- endif -%}
    </div>
  {%- endif -%}
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const wrapper = document.getElementById('InteractiveText-{{ section.id }}');
    const buttons = wrapper.querySelectorAll('.it-reveal-button');
    
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          // Staggered reveal
          buttons.forEach((btn, index) => {
            setTimeout(() => {
              btn.classList.add('is-visible');
            }, index * 200 + 300); // Base delay + stagger
          });
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.4 });

    observer.observe(wrapper);
  });
</script>

{% schema %}
{
  "name": "Interactive Text Reveal",
  "settings": [
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "button_bg",
      "label": "Button Background",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "button_text",
      "label": "Button Text",
      "default": "#FFFFFF"
    },
    {
      "type": "header",
      "content": "Typography"
    },
    {
      "type": "range",
      "id": "font_size_desktop",
      "min": 40,
      "max": 120,
      "step": 2,
      "unit": "px",
      "label": "Desktop Font Size",
      "default": 80
    },
    {
      "type": "range",
      "id": "font_size_mobile",
      "min": 24,
      "max": 60,
      "step": 2,
      "unit": "px",
      "label": "Mobile Font Size",
      "default": 36
    },
    {
      "type": "header",
      "content": "Button Customization"
    },
    {
      "type": "range",
      "id": "button_font_ratio",
      "min": 0.2,
      "max": 1.0,
      "step": 0.1,
      "label": "Button Font Size Ratio",
      "info": "Size relative to the main text (e.g., 0.5 is half the size).",
      "default": 0.4
    },
    {
      "type": "range",
      "id": "button_padding_v",
      "min": 0.2,
      "max": 2.0,
      "step": 0.1,
      "label": "Vertical Padding (em)",
      "default": 0.4
    },
    {
      "type": "range",
      "id": "button_padding_h",
      "min": 0.5,
      "max": 3.0,
      "step": 0.1,
      "label": "Horizontal Padding (em)",
      "default": 1.0
    },
    {
      "type": "range",
      "id": "button_min_width",
      "min": 50,
      "max": 200,
      "step": 10,
      "unit": "px",
      "label": "Minimum Button Width",
      "default": 0
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "max_width",
      "min": 800,
      "max": 1600,
      "step": 50,
      "unit": "px",
      "label": "Max Width",
      "default": 1200
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Top Padding",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "header",
      "content": "Mobile Spacing"
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Mobile Top Padding",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Mobile Bottom Padding",
      "default": 40
    },
    {
      "type": "header",
      "content": "Footer Text"
    },
    {
      "type": "richtext",
      "id": "subtext",
      "label": "Subtext",
      "default": "<p>Lunaria is a premium beauty studio for women, offering expert care for skin, body, and hair.</p>"
    },
    {
      "type": "url",
      "id": "read_more_link",
      "label": "Read More Link"
    },
    {
      "type": "text",
      "id": "read_more_label",
      "label": "Read More Label",
      "default": "Read More"
    }
  ],
  "blocks": [
    {
      "type": "text_segment",
      "name": "Text Segment",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "WE HELP CREATE"
        }
      ]
    },
    {
      "type": "reveal_button",
      "name": "Reveal Button",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "Button Label",
          "default": "ABOUT US"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Button Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Interactive Text Reveal",
      "blocks": [
        { "type": "text_segment", "settings": { "text": "WE" } },
        { "type": "reveal_button", "settings": { "label": "ABOUT US" } },
        { "type": "text_segment", "settings": { "text": "HELP CREATE" } },
        { "type": "text_segment", "settings": { "text": "MOMENTS OF BEAUTY" } }
      ]
    }
  ]
}
{% endschema %}
